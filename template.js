const firstname = "justin";
const lastname = "TimberLake";

// " "(white space) use for space
const fullname = firstname + " " + lastname;
console.log(fullname);

//``(caret sign) use for space in es6
const fullname2 = `${firstname} ${lastname} is a good boy.`;
console.log(fullname2);

// `` & $ can plus,minus,multiply,divide in one line
const fullname3 = `${firstname} ${80+90+10} is a good boy.`;
console.log(fullname3); 

// `` & enter create multiple line
const multipleLine = `i hate you
i don't need you
i kick you.`
console.log(multipleLine);

    const h = holee ${holeee}
    console.log(h);